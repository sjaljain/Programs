#include<iostream>
#include<vector>

using namespace std;

int main()
{
  int T;
  cin>>T;
  while(T--)
    {
      int N;
      cin>>N;
      vector<int> v(N,0),min(N,0);
      for(int i=0;i<N;i++)cin>>v[i];
      int max_diff=0;

      

      if(!max_diff)cout<<"UNFIT"<<endl;
      else cout<<max_diff<<endl;
    }
  return 0;
}
